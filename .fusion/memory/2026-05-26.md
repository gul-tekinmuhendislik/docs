# Daily Memory 2026-05-26

<!-- Append running observations, open loops, and day-to-day notes here. Promote evergreen facts to MEMORY.md. -->

## 2026-05-26: Initial project exploration

- Project is a **Mintlify documentation site** for Gül-tekin Mühendislik (Bodrum-based Turkish engineering company)
- Structure: `docs.json` (Mintlify config), ~15 service pages in `hizmetlerimiz/`, ~43+ brand pages in `markalar/`, `custom.css` with link-card/brand-grid/badge components, `scripts/` with NotebookLM enrichment pipeline and meta-description generator
- ENV: Fixed REPO_DIR=/Users/ebartan/gul-tekin-docs, DB_URL hardcoded in daily-docs-enrichment.sh
- FN-001 specified as codebase audit — read-only, deliverable is structured report saved as task document

## FN-019 specified — Brand Pages İlgili Hizmetlerimiz

**Scope:** Add a new `## İlgili Hizmetlerimiz` CardGroup section (with internal Mintlify `<Card>` links) AFTER the last external `## 🌟 ... Hizmetlerimiz` `class="link-card featured"` section on 23 brand pages.

**Key pattern:** Each external heading maps to a specific internal service slug:
- `Isı Pompası Hizmetlerimiz` → Isı Pompaları → `/hizmetlerimiz/isi-pompalari`
- `Yerden Isıtma Hizmetlerimiz` → Yerden Isıtma Sistemleri → `/hizmetlerimiz/yerden-isitma-sistemleri`
- `Güneş Enerjisi Hizmetlerimiz` → Güneş Enerjisi Sistemleri → `/hizmetlerimiz/gunes-enerjisi`
- `Bodrum Hidrofor Hizmetlerimiz` → Hidrofor Sistemleri → `/hizmetlerimiz/hidrofor-sistemleri`
- `Su Deposu Hizmetlerimiz` → Su Depoları → `/hizmetlerimiz/su-depolari`

**Placement:** Always AFTER the last `</a>` of the last external link-card on the page. One blank line before the new heading, one after `</CardGroup>`.

**Card format:** `<Card title="Service Name" href="/hizmetlerimiz/{slug}"></Card>` — no `icon` attribute.

## 2026-05-26: FN-025 specified — KISA MARKALAR Grup A (aqualine, nobo, radyal, rain-bird)

- Approved spec written to `.fusion/tasks/FN-025/PROMPT.md`
- 4 brand page enrichments (≥2000 bytes each) with header images, Hakkında, Ürün Grupları, Gül-tekin Mühendislik ile İlişkimiz, Bodrum'daki uygulamalar, İlgili Hizmetlerimiz CardGroup, and 🌟 external link-card sections
- CRITICAL: radyal.mdx title fix from "Radyal Vanalar ve Pompa Sistemleri" → "Radyal Alüminyum Radyatör ve Havlupan Sistemleri"
- Service page Card hrefs: aqualine→su-aritma, pompalar, hidrofor; nobo→akilli-oda-termostatlari, isi-pompalari; radyal→radyator, yerden-isitma, mekanik-sihhi-tesisat; rain-bird→sulama, pompalar, hidrofor
- Dependency: FN-019 (CardGroup syntax reference), consumed by FN-022 (verification coordinator)
